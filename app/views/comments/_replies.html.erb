<%= turbo_frame_tag "#{dom_id(comment)}_replies" do %>
  <%= link_to "<i class='bi bi-arrow-return-right'></i> #{display_comment_replies_count(comment.children.count)}".html_safe, blog_comments_path(blog, parent_id: comment.id), class: "fw-semibold btn-link text-muted text-decoration-none" %>
<% end %>